<!DOCTYPE html><html>
<head>
    <title>OCR Jobs | {{DISPLAY_NAME}}</title>

    <meta charset=utf-8>
    <meta name=viewport content="width=device-width">
    <meta name=referrer content=strict-origin>
    <meta http-equiv=refresh content=3>

    <link rel=stylesheet href=/style/main.css>
</head>

<body id=jobs>
    <a href="/ocr/clear">Cancel all</a>

    <table>
        <tr>
            <th>Folder</th>
            <th>Progress</th>
            <th>Actions</th>
        </tr>

        {% for job in queue %}
            <tr>
                <td>{{job}}</td>
                <td>{% if job.running %}► {% endif %}{{job.progress_text}}</td>
                <td class=actions>
                    <a href="/ocr/shift/-1/{{job}}" alt="Move up">🠋</a>
                    <a href="/ocr/shift/+1/{{job}}" alt="Move down">🠉</a>
                    <a href="/ocr/move/0/{{job}}" alt="Process now ">⭱</a>
                    <a href="/ocr/move/end/{{job}}" alt="Process last">⭳</a>
                    <a href="/ocr/cancel/{{job}}" alt="Cancel">✖</a>
                    <a
                        href="/ocr/cancel/{{job}}?recursive=1"
                        alt="Cancel this and all subfolders recursively"
                    >〷</a>
                </td>
            </tr>
        {% endfor %}
    </table>

    {% if not queue %}
        <p id=empty-queue>Queue is empty</p>
    {% endif %}
</body></html>
